import 'package:flutter/material.dart';

class ManageDrinkScreen extends StatelessWidget {
  const ManageDrinkScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      
    );
  }
}